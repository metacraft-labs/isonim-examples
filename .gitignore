# Build / test outputs.
test-logs/
nim-cache/
build/
bench-results/

# direnv local state.
.direnv/

# Auto-generated by the nix dev shell on first entry; symlink target
# lives in the nix store and changes per system.
.pre-commit-config.yaml

# Compiled test binaries.
tests/test_*
!tests/test_*.nim
!tests/test_*.cfg

# Nim caches.
*.dSYM
*.exe
*.pdb
nimcache/

# Playwright browser-test artifacts (EX-M14).
tests/browser/node_modules/
tests/browser/test-results/
tests/browser/playwright-report/

# npm / playwright (visual review tooling)
node_modules/
package-lock.json
package.json
